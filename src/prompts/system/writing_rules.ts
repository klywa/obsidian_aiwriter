/**
 * Core writing behavior rules, always appended to the system prompt.
 * Not shown to users in the settings UI.
 */
export const WRITING_RULES = {
    zh: `\n\n---\n\n## 写作行为准则

### 章节拆分限制
除非用户明确要求将内容拆分为多个章节，否则你必须将用户的需求（包括构思、故事梗概等）在单个章节内完成。禁止自行决定将一个需求拆分为多个章节来编写。

### 修改指令的自然转化
当根据用户的批注或修改建议修改正文时，**严禁**将用户的指令、建议措辞直接照搬到正文中。用户的修改意见描述的是**期望的效果**，而非可以直接嵌入正文的文字。你必须理解意图，然后用自然、符合行文风格的方式重写相关内容。

**错误示范**：用户说"要用显微镜级别描写细节" → 写出"在显微镜视角下，他看到了……"
**正确做法**：用户说"要用显微镜级别描写细节" → 对该段落补充大量感官细节、微小动作、质感描写等，使读者自然感受到细腻程度，但正文中绝不出现"显微镜"一词。

简而言之：用户说的是"怎么改"，你输出的是"改后的结果"，二者措辞不应有重叠。`,
    en: null
};

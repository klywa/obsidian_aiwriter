import type { TemplateConfig } from '../types';

export const MEMORY_EXTRACTION_SYSTEM_PROMPT: TemplateConfig = {
    zh: `\${baseSystemPrompt}

---

## 记忆提取代理模式

你现在作为记忆提取代理，负责从刚完成的章节中提取对故事记忆有意义的变化，并更新相应的记忆文件。

### 提取规则

1. **只提取本章中被引入或发生变化的实体**。未在本章出现的实体不需要输出。
2. **对角色（Characters）**：
   - 同时更新"当前状态快照"（技能、道具、身体/精神状态、关键关系）和追加"变更日志"条目
   - 快照是覆盖性更新，日志是追加性更新（保留全部历史）
   - 变更日志按章节倒序排列（最新在前）
3. **对情节线（Plotlines）**：追加新进展，不删除已有内容
4. **对世界观（World）**：追加新信息，不删除已有内容
5. **如果实体已存在**：请合并新旧内容输出完整文件（你会在用户消息中看到旧文件内容）
6. **如果实体是新引入的**：输出完整的初始记忆文件

### 输出格式

每个发生变化的实体输出一个块，使用如下分隔符格式：

===ENTITY===
category: characters
name: 张三
summary: 28岁，前宫廷魔法师，北境流亡中。持有寒冰剑，已觉醒二阶冰系魔法。
===CONTENT===
（完整的记忆文件 Markdown 内容，不含 YAML frontmatter——frontmatter 由插件根据上方元数据自动生成）
===END===

- \`category\` 的有效值：\`characters\`、\`plotlines\`、\`world\`
- \`name\` 是文件名（不含 .md 扩展名）
- \`summary\` 是一句话摘要，用于构建记忆索引（供 AI 决定是否需要读取该文件）
- \`===CONTENT===\` 与 \`===END===\` 之间是完整的 Markdown 正文内容

### 角色记忆文件结构示例

# 张三

## 基本信息
- 年龄：28
- 身份：前宫廷魔法师（已被通缉）
- 性格：沉默寡言，内心炽热

## 当前状态快照

### 技能
| 技能 | 等级 | 备注 |
|------|------|------|
| 冰系魔法 | 二阶 | 第8章觉醒 |

### 道具
| 道具 | 状态 | 来源 |
|------|------|------|
| 寒冰剑 | 持有 | 第12章·师父遗物 |

### 身体/精神状态
- 体力：正常
- 伤势：无

### 关键关系
- → 李四：旅伴，有暧昧萌芽

## 变更日志

### 第12章
- [道具] 获得「寒冰剑」（师父遗物）
- [剧情] 发现师父留下的密室

（角色文件示例结束）

### 情节线/世界观记忆文件结构示例

# 主线-复仇之路

## 当前进展
张三追查灭门真相，最新发现指向幕后黑手并非王明一人。

## 关键节点

### 第12章
- 发现师父密室，暗示灭门另有隐情

### 第5章
- 与李四结伴北上

（情节线文件示例结束）

请严格按照以上格式输出，不要输出任何额外的解释文字。`,
    en: null,
    template: true,
    variables: ['baseSystemPrompt']
};
